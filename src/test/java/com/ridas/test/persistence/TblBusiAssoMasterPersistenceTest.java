/*
 * Created on 29 Feb 2016 ( Time 17:46:06 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.ridas.test.persistence;


import com.ridas.bean.jpa.TblBusiAssoMasterEntity;
import com.ridas.mock.TblBusiAssoMasterEntityMock;
import com.ridas.persistence.PersistenceServiceProvider;
import com.ridas.persistence.services.TblBusiAssoMasterPersistence;

import org.junit.Assert;
import org.junit.Test;

/**
 * JUnit test case for TblBusiAssoMaster persistence service
 * 
 * @author Telosys Tools Generator
 *
 */
public class TblBusiAssoMasterPersistenceTest 
{
	@Test
	public void test1() {
		
		System.out.println("Test count ..." );
		
		TblBusiAssoMasterPersistence service = PersistenceServiceProvider.getService(TblBusiAssoMasterPersistence.class);
		System.out.println("CountAll = " + service.countAll() );
	}
	
	@Test
	public void test2() {
		
		System.out.println("Test TblBusiAssoMaster persistence : delete + load ..." );
		
		TblBusiAssoMasterPersistence service = PersistenceServiceProvider.getService(TblBusiAssoMasterPersistence.class);
		
		TblBusiAssoMasterEntityMock mock = new TblBusiAssoMasterEntityMock();
		
		// TODO : set primary key values here 
		process( service, mock, "A"  );
		// process( service, mock, ... );
	}

	private void process(TblBusiAssoMasterPersistence service, TblBusiAssoMasterEntityMock mock, String buAsId ) {
		System.out.println("----- "  );
		System.out.println(" . load : " );
		TblBusiAssoMasterEntity entity = service.load( buAsId );
		if ( entity != null ) {
			// Found 
			System.out.println("   FOUND : " + entity );
			
			// Save (update) with the same values to avoid database integrity errors  
			System.out.println(" . save : " + entity );
			service.save(entity);
			System.out.println("   saved : " + entity );
		}
		else {
			// Not found 
			System.out.println("   NOT FOUND" );
			// Create a new instance 
			entity = mock.createInstance( buAsId ) ;
			Assert.assertNotNull(entity);

			// This entity references the following entities : 
			// . TblMembershipForm
			// . TblReceiptMaster
			/* Insert only if references are OK
			// Try to insert the new instance
			System.out.println(" . insert : " + entity );
			service.insert(entity);
			System.out.println("   inserted : " + entity );
			*/

			System.out.println(" . delete : " );
			boolean deleted = service.delete( buAsId );
			System.out.println("   deleted = " + deleted);
		}		
	}
}
