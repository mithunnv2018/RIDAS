
				

/*
 * Created on 29 Feb 2016 ( Time 17:45:55 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.ridas.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;
//done as on 2 feb 2016
import org.springframework.web.multipart.MultipartFile;
import javax.servlet.http.HttpServletResponse;
import org.springframework.web.multipart.support.DefaultMultipartHttpServletRequest;
import java.io.File;

//--- Common classes
import com.ridas.web.common.AbstractController;
import com.ridas.web.common.FormMode;
import com.ridas.web.common.Message;
import com.ridas.web.common.MessageType;

//--- Entities
import com.ridas.bean.TblMembershipForm;

//--- Services 
import com.ridas.business.service.TblMembershipFormService;

//--- List Items 
import com.ridas.web.listitem.TblMembershipFormListItem;

/**
 * Spring MVC controller for 'TblMembershipForm' management.
 */
@Controller
@RequestMapping("/tblMembershipForm")
public class TblMembershipFormController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "tblMembershipForm";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "tblMembershipForm/form";
	private static final String JSP_LIST   = "tblMembershipForm/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/tblMembershipForm/create";
	private static final String SAVE_ACTION_UPDATE   = "/tblMembershipForm/update";

	//--done as on 19 feb 2016 to import CSV FILE Logic
	private static final String XLS_ACTION_UPLOAD = "fileImportAction";
	private static final String XLS_ACTION_UPLOAD_URL = "/tblMembershipForm/csvimport";
	private static final String JSP_SHOW_IMPORT_PAGE = "tblMembershipForm/fileimport";
	
	//--- Main entity service
	@Resource
    private TblMembershipFormService tblMembershipFormService; // Injected by Spring
	//--- Other service(s)

	//done as on 2 feb 2016
	static final String folderName="TblMembershipForm";
	private MultipartFile file2=null;
//done as on 19 feb 2016 added logic for csv file import
	private MultipartFile fileimport2 = null;
	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public TblMembershipFormController() {
		super(TblMembershipFormController.class, MAIN_ENTITY_NAME );
		log("TblMembershipFormController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "TblMembershipForm"
	 * @param model
	 */
	private void populateListOfTblMembershipFormItems(Model model) {
		List<TblMembershipForm> list = tblMembershipFormService.findAll();
		List<TblMembershipFormListItem> items = new LinkedList<TblMembershipFormListItem>();
		for ( TblMembershipForm tblMembershipForm : list ) {
			items.add(new TblMembershipFormListItem( tblMembershipForm ) );
		}
		model.addAttribute("listOfTblMembershipFormItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param tblMembershipForm
	 */
	private void populateModel(Model model, TblMembershipForm tblMembershipForm, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, tblMembershipForm);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfTblMembershipFormItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfTblMembershipFormItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of TblMembershipForm found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<TblMembershipForm> list = tblMembershipFormService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new TblMembershipForm
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		TblMembershipForm tblMembershipForm = new TblMembershipForm();	

// done as on 10 feb 2016  save primary key default to temp
				String DUMMY="temp";
		tblMembershipForm.setMemberId(DUMMY);

		populateModel( model, tblMembershipForm, FormMode.CREATE);


		//done as on 2 feb 2016
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing TblMembershipForm
	 * @param model Spring MVC model
	 * @param memberId  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{memberId}")
	public String formForUpdate(Model model, @PathVariable("memberId") String memberId ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		TblMembershipForm tblMembershipForm = tblMembershipFormService.findById(memberId);
		populateModel( model, tblMembershipForm, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param tblMembershipForm  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid TblMembershipForm tblMembershipForm, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest,HttpServletResponse httpServletResponse) {
		//done as on 2 feb 2016
		//ADD PARAMS HttpServletResponse httpServletResponse

		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {

				

// done as on 06 feb 2016  save primary key default to temp
				String DUMMY="temp";

				tblMembershipForm.setMemberId(DUMMY);

				TblMembershipForm tblMembershipFormCreated = tblMembershipFormService.create(tblMembershipForm); 
				model.addAttribute(MAIN_ENTITY_NAME, tblMembershipFormCreated);

				
				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				// done as on 06 feb 2016  introduced a new getter for getting the primary key of the newly created Bean coming after saved.
				return redirectToForm(httpServletRequest, tblMembershipFormCreated.getMemberId() );
				//return redirectToForm(httpServletRequest, tblMembershipForm.getMemberId() );
			} else {
				populateModel( model, tblMembershipForm, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "tblMembershipForm.error.create", e);
			populateModel( model, tblMembershipForm, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param tblMembershipForm  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid TblMembershipForm tblMembershipForm, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest,HttpServletResponse httpServletResponse) {
	//done as on 2 feb 2016
	//ADD PARAMS HttpServletResponse httpServletResponse
		
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {

				
				//--- Perform database operations
				TblMembershipForm tblMembershipFormSaved = tblMembershipFormService.update(tblMembershipForm);
				model.addAttribute(MAIN_ENTITY_NAME, tblMembershipFormSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, tblMembershipForm.getMemberId());
			
				
				
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, tblMembershipForm, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "tblMembershipForm.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, tblMembershipForm, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param memberId  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{memberId}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("memberId") String memberId) {
		log("Action 'delete'" );
		try {
			tblMembershipFormService.delete( memberId );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "tblMembershipForm.error.delete", e);
		}
		return redirectToList();
	}

	@RequestMapping("/formforcsvimport2")
	public String formForExcelImport(Model model) {
		log("Action 'formforexcelimport'");
		// --- Populates the model with a new instance
		TblMembershipForm tblMembershipForm = new TblMembershipForm();

		// done as on 19 feb 2016  save primary key default to temp
				String DUMMY="temp";

		tblMembershipForm.setMemberId(DUMMY);


		model.addAttribute("fileimport2", fileimport2);
		 
		model.addAttribute(XLS_ACTION_UPLOAD, XLS_ACTION_UPLOAD_URL);

		// done as on 2 feb 2016
		return JSP_SHOW_IMPORT_PAGE;
	}

	@RequestMapping("/csvimport")
	public String excelimport(TblMembershipForm tblMembershipForm, BindingResult bindingResult, Model model,
			RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest,
			HttpServletResponse httpServletResponse) {
		 
		log("Action 'csvuploading'");
		try {
			if (!bindingResult.hasErrors()) {

				 
				file2 = (MultipartFile) ((DefaultMultipartHttpServletRequest) httpServletRequest).getFile("fileimport2");
				if (file2.isEmpty() == false) {
 					String productPdfFileName = file2.getOriginalFilename();
					 
 					String contenttype = file2.getContentType();
 					
 					tblMembershipFormService.doImportExcelSpreadSheet(file2);	
 
				}
			}
		} catch (Exception e) {
			log("Action 'CSVImport' : Exception - " + e.getMessage());
			messageHelper.addException(model, "tblMembershipForm.error.create", e);	
		}
		return "redirect:/"+MAIN_ENTITY_NAME;
	}
}
